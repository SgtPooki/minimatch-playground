{
  "name": "minimatch-playground",
  "version": "1.0.0",
  "private": false,
  "dependencies": {
    "minimatch": "^3.0.4",
    "react": "^16.4.1",
    "react-contenteditable": "^2.1.0",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "prebuild": "yarn lint && yarn test",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "lint": "prettier '**/*.{js,css,json,html}' --list-different",
    "fix": "prettier '**/*.{js,css,json,html}' --write"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
